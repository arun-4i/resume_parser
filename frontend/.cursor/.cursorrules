# .cursor/.cursorrules — Frontend Rules Router & Workflow

Main entry for all frontend rules, standards, and plans. Start here for any new feature, refactor, or process change.

## Essential Workflow

- **Terminal:** Always use PowerShell commands only (no Command Prompt)
- **Documentation:** Always use Context7 MCP tool for latest, up-to-date APIs
- **Process:** Follow rule-specific guidelines; update rules when patterns change

## Rule Index

| Category               | Rule File                            | Purpose                                      |
| ---------------------- | ------------------------------------ | -------------------------------------------- |
| **Core Development**   | `.cursor/rules/frontend-dev.mdc`     | TypeScript, components, error handling, a11y |
| **Code Quality**       | `.cursor/rules/frontend-linting.mdc` | ESLint, Prettier, hooks, strict mode         |
| **Validation**         | `.cursor/rules/validation.mdc`       | Zod schemas, input sanitization              |
| **UI & Layout**        | `.cursor/rules/css-layout.mdc`       | Z-index, scroll, borders, responsive         |
| **Animations**         | `.cursor/rules/animation-framer.mdc` | Framer Motion vs Tailwind transitions        |
| **Responsive Design**  | `.cursor/rules/responsiveness.mdc`   | Mobile-first, breakpoints, device testing    |
| **State Management**   | `.cursor/rules/state.mdc`            | URL state (nuqs), local state, memoization   |
| **React Hooks**        | `.cursor/rules/hooks.mdc`            | usehooks-ts, custom hooks, best practices    |
| **Rendering Strategy** | `.cursor/rules/rendering.mdc`        | SSR/ISR/CSR decision workflow                |
| **Security**           | `.cursor/rules/security.mdc`         | Encrypt/decrypt middleware, input validation |
| **Workflow**           | `.cursor/rules/workflow.mdc`         | Terminal usage, documentation access         |

## Rule Change Workflow

1. Propose changes via PR
2. AI suggests best practices, user will review it and take suggestions.
3. User has final approval
4. Update this router and affected rule files

---

**Quick Reference:** Each rule file contains specific technical standards. Use Context7 MCP for latest documentation. PowerShell commands only in terminal.
